const path = require("path");

let config = {

    mode: "none", 
    devtool: "source-map",
    entry: "./src/leaflet.canvaslabel.js",
    output: {
        filename: 'Leaflet.canvaslabel.js',
        path: path.resolve(__dirname, 'dist'),
        libraryTarget: 'umd'
    }
};

module.exports = config;