const path = require("path");
const UglifyJsPlugin = require('uglifyjs-webpack-plugin');

let config = {

    mode: "none", 
    entry: "./src/leaflet.canvaslabel.js",
    output: {
        filename: 'Leaflet.canvaslabel.min.js',
        path: path.resolve(__dirname, 'dist'),
        libraryTarget: 'umd'
    },
    plugins:[
        new UglifyJsPlugin()
    ]

};

module.exports = config;